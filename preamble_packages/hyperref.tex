\usepackage[
  draft=false,
%  raiselinks = {false},%%  default:{true}
  breaklinks = {true},%%   default:{false} Erlaubt Zeilenumbrche in Links
%  pageanchor = {false},%%  default:{true} implicit anchor at top left corner
%  plainpages = {true},%%   default:{false} verwendet nur arabische Seiten-Label
  linktoc = {all},%%       default:{section} (section|page|all|none)
%  linktocpage = {true},%%  default:{false} Seitennummer, nicht Text, ist Link
%  colorlinks = {true},%%   default:{} Benutze farbige Links
%  allcolors = {},
  hidelinks,
]{hyperref}%% Extensive support for hypertext in LaTeX

\usepackage[draft=false]{bookmark}%% bookmark organization for hyperref
