%%==============================================================================
%% Citations and References
%%==============================================================================
\usepackage[
  autostyle=true,%%   default: tryonce
  german=quotes,%%    syle variants for german: quotes, guillements, swiss
  english=british,%%  syle variants for english: american, british
  french=quotes,%%    syle variants for french: quotes, quotes*,
%%                                              guillements, guillements*
%  maxlevel=2,%%       default: 2
%  threshold=3,%%      default: 3
]{csquotes}%% Context sensitive quotation facilities

%\SetCiteCommand{\citep}% specifies a replacement for \cite in
%%csquote-commands

%% set block quote to smaller text size
%\RequirePackage{relsize}
%\newenvironment*{smallquote}
% {\quote\smaller}
% {\endquote}
%\SetBlockEnvironment{smallquote}
