%% === The LaTeX standard graphics bundle ===

%\usepackage[]{color}%% Colour control for LaTeX documents

%\usepackage[]{graphics}%% The standard graphics package

\usepackage[
%  final,
%  hiderotate,%% Do not show rotated text
%  hidescale,%% Do not show scaled text
%  hiresbb,%% Look for size specifications in %%HiResBoundingBox
]{graphicx}         %% The extended or enhanced graphics package

%\usepackage{trig}%% Simple trigonometric functions

%\usepackage{epsfig}%% Include Encapsulated PostScript in LaTeX documents

%\usepackage{keyval}%% Process 'key=value' schemes

\usepackage{lscape}%% Place selected parts of a document in landscape
