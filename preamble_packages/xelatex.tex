%% === XeLaTeX ===
%% contains packages with LaXeTeX, material supporting it, and packages
%% requiring it. 

\usepackage{fontspec}%% Advanced font selection in XeLaTeX and LuaLaTeX
\KOMAoptions{DIV=last}%%    recalc DIV when Font is loaded